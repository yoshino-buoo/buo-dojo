{
  "name": "buo-horagai-dojo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "build:preview": "node scripts/render-link-preview.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:layout": "playwright test",
    "check": "node --check app.js && node --check touch.js && node --check breath.js && node --check haptics.js && node --check config.js && node --check share.js && node --check share-card.js && node --check analytics.js && node --check analytics-worker/worker.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@playwright/test": "1.55.1"
  }
}
